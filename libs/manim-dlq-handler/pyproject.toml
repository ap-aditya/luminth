[project]
name = "manim-dlq-handler"
version = "0.1.0"
description = "Cloud Function to handle dead-letter queue of rendering-service"
requires-python = ">=3.11"
dependencies = [
    "cloudevents>=1.12.0",
    "functions-framework>=3.8.3",
    "redis>=6.2.0",
]
